# Provider live acceptance plan

## Acceptance and isolation

Ron requires seamless operations across Claude, Codex, Gemini and Qwen. Provider capability gaps are defects to close, not N/A results. Vendor allowance is the availability gate for installed, signed-in agents; missing CLI/authentication are guided setup states. User stop, privacy, project ownership and corrupt/missing conversation identity remain correctness controls. Never silently change vendor or model.

Install Clayrune first; the in-app chooser supports multiple selected vendors, installs every selected CLI harness and its prerequisites, and guides sign-in for each. The user explicitly selects one default; the others remain immediately available per agent and chat. Preserve explicit unattended installer options. Do not restore an install-time interactive vendor picker.

All second instances MUST set `MC_REMOTE_ENABLED=0` BEFORE imports, use disposable `MC_DATA_DIR`, isolated configuration/native homes and a separate loopback-only port. Register owned PIDs and retain cleanup evidence. Do not restart port 5199. No synthetic quota fixture counts as an actually observed vendor error. Existing non-Codex credits are unavailable per Ron; do not spend on those providers without updated authorization.

## Flows × providers

Each evidence path below is a REQUIRED destination, not a claim that a file exists. Status starts **UNVERIFIED**. Fill with command, exit code, CLI version, identities, sanitized request/response/terminal trace and relevant UI screenshot. Keep operator transcripts and credentials outside tracked source. Mark production checks **needs live restart** until Dave obtains Ron's go-ahead; do not use that label for work still runnable in isolation.

| Flow | Claude | Codex | Gemini | Qwen |
|---|---|---|---|---|
| multi-vendor-first-run | UNVERIFIED (clean VM not reached; smoke-level backend+UI evidence only): Clean machine without Node or CLIs: select Claude plus another vendor, install/authenticate both, choose default, and chat with an agent on each. Evidence: `docs/_journal/provider-live/claude/multi-vendor-first-run.md`, `docs/_journal/provider-live/w6-smoke-evidence.md` | UNVERIFIED (clean VM not reached; smoke-level backend+UI evidence only; Codex install/auth-detection covered without spending allowance): Clean machine without Node or CLIs: select Codex plus another vendor, install/authenticate both, choose default, and chat with an agent on each. Evidence: `docs/_journal/provider-live/codex/multi-vendor-first-run.md`, `docs/_journal/provider-live/w6-smoke-evidence.md` | UNVERIFIED (clean VM not reached; smoke-level backend+UI evidence only): Clean machine without Node or CLIs: select Gemini plus another vendor, install/authenticate both, choose default, and chat with an agent on each. Evidence: `docs/_journal/provider-live/gemini/multi-vendor-first-run.md`, `docs/_journal/provider-live/w6-smoke-evidence.md` | UNVERIFIED (clean VM not reached; smoke-level backend+UI evidence only): Clean machine without Node or CLIs: select Qwen plus another vendor, install/authenticate both, choose default, and chat with an agent on each. Evidence: `docs/_journal/provider-live/qwen/multi-vendor-first-run.md`, `docs/_journal/provider-live/w6-smoke-evidence.md` |
| first-run | UNVERIFIED (clean VM not reached; prerequisite-installer unit tests + real-instance route wiring only): CLI absent: install core, select vendor in app, install selected CLI, sign in, send marker, verify reply and selected vendor. Evidence: `docs/_journal/provider-live/claude/first-run.md`, `docs/_journal/provider-live/w6-smoke-evidence.md` | UNVERIFIED (clean VM not reached; install/auth-detection code path verified without spending allowance): CLI absent: install core, select vendor in app, install selected CLI, sign in, send marker, verify reply and selected vendor. Evidence: `docs/_journal/provider-live/codex/first-run.md`, `docs/_journal/provider-live/w6-smoke-evidence.md` | UNVERIFIED (clean VM not reached; prerequisite-installer unit tests + real-instance route wiring only): CLI absent: install core, select vendor in app, install selected CLI, sign in, send marker, verify reply and selected vendor. Evidence: `docs/_journal/provider-live/gemini/first-run.md`, `docs/_journal/provider-live/w6-smoke-evidence.md` | UNVERIFIED (clean VM not reached; prerequisite-installer unit tests + real-instance route wiring only, incl. pip/uv bootstrap for Aider): CLI absent: install core, select vendor in app, install selected CLI, sign in, send marker, verify reply and selected vendor. Evidence: `docs/_journal/provider-live/qwen/first-run.md`, `docs/_journal/provider-live/w6-smoke-evidence.md` |
| new-chat | UNVERIFIED: Create two parallel chats in one project; verify distinct IDs, visible rail rows and no mixed messages. Evidence: `docs/_journal/provider-live/claude/new-chat.md` | UNVERIFIED: Create two parallel chats in one project; verify distinct IDs, visible rail rows and no mixed messages. Evidence: `docs/_journal/provider-live/codex/new-chat.md` | UNVERIFIED: Create two parallel chats in one project; verify distinct IDs, visible rail rows and no mixed messages. Evidence: `docs/_journal/provider-live/gemini/new-chat.md` | UNVERIFIED: Create two parallel chats in one project; verify distinct IDs, visible rail rows and no mixed messages. Evidence: `docs/_journal/provider-live/qwen/new-chat.md` |
| follow-up | UNVERIFIED: Send second marker; verify same thread and exact requested model/effort. Evidence: `docs/_journal/provider-live/claude/follow-up.md` | UNVERIFIED: Send second marker; verify same thread and exact requested model/effort. Evidence: `docs/_journal/provider-live/codex/follow-up.md` | UNVERIFIED: Send second marker; verify same thread and exact requested model/effort. Evidence: `docs/_journal/provider-live/gemini/follow-up.md` | UNVERIFIED: Send second marker; verify same thread and exact requested model/effort. Evidence: `docs/_journal/provider-live/qwen/follow-up.md` |
| restart-resume | UNVERIFIED: Restart isolated instance; reopen and follow up; verify same native identity or explicit lossless Clayrune-managed resume with preserved history. Evidence: `docs/_journal/provider-live/claude/restart-resume.md` | UNVERIFIED: Restart isolated instance; reopen and follow up; verify same native identity or explicit lossless Clayrune-managed resume with preserved history. Evidence: `docs/_journal/provider-live/codex/restart-resume.md` | **PASS (2026-09-18, W4, live):** `provider_session_id` capture fixed (was written to a private key nothing persisted) and `--resume` threaded into a COLD `build_command()`/`dispatch()`; `gemini` added to `_COLD_RESUMABLE_PROVIDERS`. Live: turn 1 memorized a code, in-memory session discarded entirely (restart simulation), a brand-new cold `dispatch(resume_id=...)` correctly recalled it. Evidence: `docs/_journal/provider-live/gemini/restart-resume.md` | UNVERIFIED: Restart isolated instance; reopen and follow up; verify same native identity or explicit lossless Clayrune-managed resume with preserved history. Evidence: `docs/_journal/provider-live/qwen/restart-resume.md` |
| image-paste | UNVERIFIED: Paste fixture image, ask about its content, verify attachment persists and answer uses image. Evidence: `docs/_journal/provider-live/claude/image-paste.md` | UNVERIFIED: Paste fixture image, ask about its content, verify attachment persists and answer uses image. Evidence: `docs/_journal/provider-live/codex/image-paste.md` | **PASS (2026-09-18, W4, live):** first attempt reproduced a real bug — `read_file`'s `isWithinRoot` check refuses any attachment path outside the workspace, exactly Clayrune's real `data/uploads/` shape for any project not an ancestor of it. `--include-directories` fix; re-test with a test image outside the cwd correctly read and described ("PURPLE ELEPHANT" in purple, matching actual drawn content). Evidence: `docs/_journal/provider-live/gemini/image-paste.md` | **PARTIAL (2026-09-18, W4, live) — NOT a pass:** same workspace-boundary fix applies (mechanism parity), but the default model (`qwen3-coder-plus`) has NO vision support. Without being told to verify, it FABRICATED a description matching the filename, not the real image, with no `read_file` call in the log at all; told explicitly to call the tool, it did and then honestly reported it can't process images. Evidence: `docs/_journal/provider-live/qwen/image-paste.md` |
| notify | PARTIAL (2026-09-18, W5): dispatch + exactly-once completion receipt verified live as Claude→Gemini and Claude→Qwen source (see `cross-dispatch` row below for the full ordered-pair matrix). Parent-continuation-**after-parent-restart** specifically was NOT exercised — no restart was performed. Evidence: `docs/_journal/provider-live/claude/notify.md` (pointer to cross-dispatch.md; restart leg still open) | UNVERIFIED (Codex out of allowance until Sep 24; no prompts sent): Dispatch child with notify_session; verify exactly one completion receipt and parent continuation after parent restart. Evidence: `docs/_journal/provider-live/codex/notify.md` | PARTIAL (2026-09-18, W5): same as Claude column — dispatch+receipt verified live as source to Claude and Qwen; restart leg not exercised. Evidence: `docs/_journal/provider-live/gemini/notify.md` | PARTIAL (2026-09-18, W5): dispatch+receipt attempted as source to Claude and Gemini but **both FAILED** — see `cross-dispatch` row; the Qwen-hosted agent could not reliably execute the shell commands needed to make the dispatch call at all, and fabricated the expected marker instead of reporting the failure. Evidence: `docs/_journal/provider-live/qwen/notify.md` |
| workflow | UNVERIFIED: Run two dependent steps; verify selected vendor/model, outputs passed once and real failure state. Evidence: `docs/_journal/provider-live/claude/workflow.md` | UNVERIFIED: Run two dependent steps; verify selected vendor/model, outputs passed once and real failure state. Evidence: `docs/_journal/provider-live/codex/workflow.md` | UNVERIFIED: Run two dependent steps; verify selected vendor/model, outputs passed once and real failure state. Evidence: `docs/_journal/provider-live/gemini/workflow.md` | UNVERIFIED: Run two dependent steps; verify selected vendor/model, outputs passed once and real failure state. Evidence: `docs/_journal/provider-live/qwen/workflow.md` |
| schedule | UNVERIFIED: Run isolated one-shot schedule; verify selected engine, execution record and completion. Evidence: `docs/_journal/provider-live/claude/schedule.md` | UNVERIFIED: Run isolated one-shot schedule; verify selected engine, execution record and completion. Evidence: `docs/_journal/provider-live/codex/schedule.md` | UNVERIFIED: Run isolated one-shot schedule; verify selected engine, execution record and completion. Evidence: `docs/_journal/provider-live/gemini/schedule.md` | UNVERIFIED: Run isolated one-shot schedule; verify selected engine, execution record and completion. Evidence: `docs/_journal/provider-live/qwen/schedule.md` |
| memory | UNVERIFIED: Write unique fact; finish Scribe; new chat read-floor retrieves it; no duplicate checkpoint or post-delete publication. Evidence: `docs/_journal/provider-live/claude/memory.md` | UNVERIFIED: Write unique fact; finish Scribe; new chat read-floor retrieves it; no duplicate checkpoint or post-delete publication. Evidence: `docs/_journal/provider-live/codex/memory.md` | UNVERIFIED: Write unique fact; finish Scribe; new chat read-floor retrieves it; no duplicate checkpoint or post-delete publication. Evidence: `docs/_journal/provider-live/gemini/memory.md` | UNVERIFIED: Write unique fact; finish Scribe; new chat read-floor retrieves it; no duplicate checkpoint or post-delete publication. Evidence: `docs/_journal/provider-live/qwen/memory.md` |
| hire | Not re-tested this pass (builder/Tobin already hired on mission_control). Evidence: `docs/_journal/provider-live/claude/hire.md` | UNVERIFIED (Codex out of allowance until Sep 24): Drag agent onto same project; verify identity, requested engine and usable conversation. Evidence: `docs/_journal/provider-live/codex/hire.md` | **VERIFIED (2026-09-18, W5, live):** `POST /api/project/mission_control/roster/hire` for `global:market-scout`; usability dispatch replied exact marker `HIRE-USABLE-GEMINI`. Left hired. Evidence: `docs/_journal/provider-live/gemini/hire.md` | **VERIFIED (2026-09-18, W5, live):** `POST /api/project/mission_control/roster/hire` for `global:marlow-q`; usability dispatch replied exact marker `HIRE-USABLE-QWEN`. Left hired. Evidence: `docs/_journal/provider-live/qwen/hire.md` |
| stop-interrupt | UNVERIFIED: Start bounded task; interrupt then stop; verify owned process cleanup, retained partial history and accurate state. Evidence: `docs/_journal/provider-live/claude/stop-interrupt.md` | UNVERIFIED: Start bounded task; interrupt then stop; verify owned process cleanup, retained partial history and accurate state. Evidence: `docs/_journal/provider-live/codex/stop-interrupt.md` | UNVERIFIED: Start bounded task; interrupt then stop; verify owned process cleanup, retained partial history and accurate state. Evidence: `docs/_journal/provider-live/gemini/stop-interrupt.md` | UNVERIFIED: Start bounded task; interrupt then stop; verify owned process cleanup, retained partial history and accurate state. Evidence: `docs/_journal/provider-live/qwen/stop-interrupt.md` |
| questions | UNVERIFIED: Ask two-option mc:question; answer in UI; verify waiting state, durable answer and same-thread continuation. Evidence: `docs/_journal/provider-live/claude/questions.md` | PARTIAL (2026-09-18, W4, offline proof only — Codex out of allowance until Sep 24): `supports_ask_user_question` flipped True on CODE-PATH PARITY with the Qwen live result (same `with_mc_tool_protocol()` call, same shared `_mode_a_reader` turn-end scan, no Codex-specific branch) — not independently live-verified. Re-verify after Sep 24. Evidence: `docs/_journal/provider-live/codex/questions.md` | Not re-tested this pass (already True; mechanism unchanged by W4). Evidence: `docs/_journal/provider-live/gemini/questions.md` | **PASS (2026-09-18, W4, live):** a real `mc:question` fence paused the turn (status→idle, `pending_questions` populated with the real question/options), and a follow-up answer resumed the SAME session (`--resume`, real `provider_session_id`) and completed correctly, choice honored. Evidence: `docs/_journal/provider-live/qwen/questions.md` |
| mcp | UNVERIFIED: Expose fixture MCP tool; invoke and verify result plus tool receipt; no vendor-specific missing capability refusal. Evidence: `docs/_journal/provider-live/claude/mcp.md` | UNVERIFIED: Expose fixture MCP tool; invoke and verify result plus tool receipt; no vendor-specific missing capability refusal. Evidence: `docs/_journal/provider-live/codex/mcp.md` | Not re-tested this pass (out of W4 scope — item 3 targeted Qwen only). Evidence: `docs/_journal/provider-live/gemini/mcp.md` | **PASS (2026-09-18, W4, live):** was unconditional deny-all (`--allowed-mcp-server-names __clayrune_none__`); now `--mcp-config` declares Clayrune's resolved per-project set (same resolver Claude uses) and `--allowed-mcp-server-names` allowlists exactly those names. Live leak-probe, real `.mcp.json` (filesystem+browser): declaring only `filesystem` connected only `filesystem` — `browser` never leaked through. `supports_mcp` flipped True. Evidence: `docs/_journal/provider-live/qwen/mcp.md` |
| usage | UNVERIFIED: Verify token counts and cost/provenance from actual exposed usage; distinguish measured, estimated and unavailable upstream data. Evidence: `docs/_journal/provider-live/claude/usage.md` | UNVERIFIED: Verify token counts and cost/provenance from actual exposed usage; distinguish measured, estimated and unavailable upstream data. Evidence: `docs/_journal/provider-live/codex/usage.md` | **PASS (2026-09-18, W4, live):** the `result` event always carried real token counts (`stats` object) but `_read_stream` never stored the payload — `session['usage']` stayed unset, `emits_usage=False` was honest about the bug not the CLI. Fixed (mirrors `_mode_a_reader`'s own TURN_END handling); live-verified `session['usage']` populated matching the CLI's own report. `cost_usd`/`num_turns` correctly stay unavailable (CLI never emits either). Evidence: `docs/_journal/provider-live/gemini/usage.md` | **PASS (2026-09-18, W4, live, already correct):** shared `_mode_a_reader` already wired this; live-reconfirmed real figures populate `session['usage']`. No fix needed. Evidence: `docs/_journal/provider-live/qwen/usage.md` |
| allowance | UNVERIFIED: Capture actual CLI quota/rate-limit text; verify named vendor/limit and reset only when exposed in Floor/chooser; no fallback. Evidence: `docs/_journal/provider-live/claude/allowance.md` | UNVERIFIED: Capture actual CLI quota/rate-limit text; verify named vendor/limit and reset only when exposed in Floor/chooser; no fallback. Evidence: `docs/_journal/provider-live/codex/allowance.md` | UNVERIFIED: Capture actual CLI quota/rate-limit text; verify named vendor/limit and reset only when exposed in Floor/chooser; no fallback. Evidence: `docs/_journal/provider-live/gemini/allowance.md` | UNVERIFIED: Capture actual CLI quota/rate-limit text; verify named vendor/limit and reset only when exposed in Floor/chooser; no fallback. Evidence: `docs/_journal/provider-live/qwen/allowance.md` |
| cross-dispatch | **PASS (2026-09-18, W5, live):** Claude→Gemini and Claude→Qwen both dispatched with `notify_session`, both returned the exact requested marker. Evidence: `docs/_journal/provider-live/claude/cross-dispatch.md` | UNVERIFIED (Codex out of allowance until Sep 24; no prompts sent). Evidence: `docs/_journal/provider-live/codex/cross-dispatch.md` | **PASS (2026-09-18, W5, live):** Gemini→Claude (real 2-hop notify wake, proven via the outer session's own transcript) and Gemini→Qwen both correct. Evidence: `docs/_journal/provider-live/gemini/cross-dispatch.md` | **PASS after fix (2026-09-18, same day):** first pass found both Qwen→Claude and Qwen→Gemini **fabricating** the marker after the Qwen-hosted agent's shell tool silently failed (root cause: `guard_shell_command()` unconditionally quoted a no-space script path, breaking Qwen's `cmd.exe`-launched hook on this server). W4 fixed it (`fb78ee6`, `w4-capability-bridges`, not on this branch — W4 owns that merge). Re-tested live on a disposable second instance with the fix applied: both pairs pass on retry (real dispatch, real notify wake, no fabrication observed even on 2 unrelated post-fix hiccups — one real, honestly-reported 415, one unexplained off-topic hallucination that didn't attempt to fabricate a marker either). Evidence: `docs/_journal/provider-live/qwen/cross-dispatch.md`, `docs/_journal/provider-live/cross-vendor/W5-notes.md` |
| mixed-workflow | **BLOCKED (2026-09-18, W5), by design — reaffirmed after pushback:** workflow definition CRUD refuses every agent caller (`workflow_routes.py:65-73`, CLAUDE.md authority guard, MC-871) — could not create a 2-step 2-vendor definition to test with, and did not attempt to bypass the guard. Dave argued a disposable second instance changes this ("you are the test operator, not an agent granting itself capability"); declined — the standing position backing this guard (`position_whetheranagentsessionmaycreateoreditworkflowdefi.md`, Ron, 2026-09-10) says explicitly "for attended and unattended sessions alike" and frames it around *what is authored*, not which instance stores it; no reopen condition was invoked. Full reasoning: `docs/_journal/provider-live/cross-vendor/W5-notes.md`. Needs a human to create one once via the UI. Mixed-vendor step mechanics and Fenn #4 (failure never becomes content) confirmed correct by code reading. Evidence: `docs/_journal/provider-live/claude/mixed-workflow.md` | UNVERIFIED (Codex out of allowance; also blocked by the same authority guard regardless). Evidence: `docs/_journal/provider-live/codex/mixed-workflow.md` | **BLOCKED, same reason** — refusal is vendor-agnostic. Evidence: `docs/_journal/provider-live/gemini/mixed-workflow.md` | **BLOCKED, same reason** — refusal is vendor-agnostic. Evidence: `docs/_journal/provider-live/qwen/mixed-workflow.md` |
| handoff | **NOT POSSIBLE (2026-09-18, W5), live-confirmed, applies to all 12 ordered pairs:** live resume attempt (my own Claude conversation, `provider:"gemini"` requested) returned the real, generic refusal `"conversation belongs to another provider; start a new chat to switch providers"` (`agent_routes.py:7085-7086`). The provider-neutral canonical-journal layer that would enable a real handoff (`conversation_views.py`) has zero production callers. This is a missing capability, not a live-test gap — one repro stands for all pairs since the refusal doesn't branch on which two vendors are named. Evidence: `docs/_journal/provider-live/claude/handoff.md` | UNVERIFIED (Codex out of allowance; also blocked by the same generic refusal regardless). Evidence: `docs/_journal/provider-live/codex/handoff.md` | **NOT POSSIBLE, same reason.** Evidence: `docs/_journal/provider-live/gemini/handoff.md` | **NOT POSSIBLE, same reason.** Evidence: `docs/_journal/provider-live/qwen/handoff.md` |
| hivemind-mixed-workers (new row, W5 — no prior row covered this flow) | **Model-default bug FIXED for real (2026-09-18, W5); `authorize_execution` gate DECLINED, not faked.** Live-found a previously-undocumented bug: the hivemind-level `worker_model` default leaks the wrong vendor's tier name into any workstream with an empty per-workstream `model` (`engine_selection.py:81` + `hivemind_routes.py`). Fixed in `_hm_spawn_worker_session` — a workstream's own `provider` now only inherits the manifest default model when it matches the manifest's own default provider; 4 new tests in `tests/test_hivemind_worker_model_default.py`, fail-before/pass-after verified (stashed the fix, reproduced the exact live error, restored). Dave asked to also route both launch sites through `execution_policy.authorize_execution()` — declined: the module's own docstring requires Certification evidence from "a trusted, versioned canary-test runner… not an agent-authored capability declaration," and no such runner exists for the INTERACTIVE profile on any vendor (only `ClaudeRuntime.transform_evidence()` for TOOL_FREE_TRANSFORM). Writing a self-declared Certification would satisfy the checkbox while proving nothing — building the real one is W1/W0's charter, not a hivemind_routes.py patch. Gate confirmed still open. Evidence: `docs/_journal/provider-live/cross-vendor/hivemind-mixed-workers.md`, `docs/_journal/provider-live/cross-vendor/W5-notes.md` | — | — | — |
| guardrail | **VERIFIED (2026-09-18, W2 redesign — per-launch injection, NOT global config):** `--settings <generated file>` argv flag, wired in `ClaudeRuntime.build_command()` (the one builder every dispatch/resume/followup path delegates to). `~/.claude/settings.json` is never written. Live-verified additive with a real pre-existing user hook (isolated home) — user's marker hook AND Clayrune's guard both fired. Real `taskkill /IM notepad.exe` via Claude's own Bash tool, generated (fixed-quoting) command: blocked, notepad survived, Claude declined to route around it. Full evidence: `docs/GUARDRAIL_PARITY_EVIDENCE.md` §4/§5. | PENDING: hooks DO exist in Codex CLI 0.154.0 (corrected from the first pass — `codex exec --help` names `--dangerously-bypass-hook-trust`); installer support and `-c hooks=`/`bypass_hook_trust` argv injection are wired in `CodexRuntime.build_command()`, but NOT independently confirmed live — no Codex launches while allowance is out. Re-check after 2026-09-24. Evidence: `docs/GUARDRAIL_PARITY_EVIDENCE.md` §1/§4. | **VERIFIED (2026-09-18, W2 redesign):** `GEMINI_CLI_SYSTEM_SETTINGS_PATH` env var, wired in `GeminiRuntime.dispatch()`/`write_followup()`. `~/.gemini/settings.json` is never written. **Caught and fixed a real bug here**: the first quoting format silently broke this vendor's hook execution (Gemini's `.cmd` launcher's shell errored on it, and a failed hook was treated as an ALLOW) — a real dispatch through the actual second-instance HTTP API killed a live test notepad before the fix. Re-verified through the same real dispatch path after the fix: blocked, notepad survived. Evidence + the bug's full writeup: `docs/GUARDRAIL_PARITY_EVIDENCE.md` §4. | **VERIFIED (2026-09-18, W2 redesign):** `QWEN_CODE_SYSTEM_SETTINGS_PATH` env var, wired in `QwenRuntime.dispatch()`/`write_followup()`. `~/.qwen/settings.json` is never written. Still required dropping `--bare` (unconditionally disabled hooks) for `--allowed-mcp-server-names __clayrune_none__` (§2). Live-verified additive with a real pre-existing user hook, and blocked a real `taskkill` with the fixed-quoting generated command; notepad survived. Evidence: `docs/GUARDRAIL_PARITY_EVIDENCE.md` §2/§4. |

## Ordered cross-vendor coverage

For cross-dispatch, mixed-workflow and handoff, each source column above must contain separate evidence for all three destination vendors: Claude→Codex/Gemini/Qwen; Codex→Claude/Gemini/Qwen; Gemini→Claude/Codex/Qwen; Qwen→Claude/Codex/Gemini. Twelve ordered pairs per flow. Same-provider checks do not substitute for any pair.

## Current evidence

- Latest independent scoped regression: 109 passed across nine Python files. This is offline component evidence, not vendor live acceptance.
- Full clean-master baseline has NOT been reproduced yet: attempted registered Start-Process launch was rejected by command approval before execution. Dave's 3271-pass/5-failure result is external evidence pending local reproduction and named failures.
- Full feature suite is NOT green: Dave reported 32 failures before stopping at 61%; needs complete run and repair.
- W6 (2026-09-17): fixed Fenn blocker #5's pip/uv half (Aider had no prerequisite bootstrap; onboarding failed closed with "unsupported" instead of installing pip/uv) — `mc/blueprints/agent_routes.py`, 3 new tests in `tests/test_agent_routes.py`, 51/51 passing. Confirmed the same gap did not exist for npm (already fixed). Did not reach the clean-VM run; see `docs/_journal/provider-live/w6-smoke-evidence.md` for what was and wasn't verified, including a disclosed isolation miss (no `MC_REMOTE_ENABLED=0` on a throwaway second instance — contained to one outbound health-check GET, no enrollment/tunnel).
- W6 (2026-09-18): `tools/smoke/first-run-provider-chooser.mjs` was failing on unmodified `c14c2fd` — reproduced on the base commit — because it read the OLD single-radio chooser DOM; rewritten for the multi-select checkbox/default-radio shape, now 10/10 assertions pass, plus 2 new multi-select-specific assertions it never had. Added `tools/smoke/first-run-signin-flow.mjs`: real second `server.py` instance (own port/data dir, `MC_REMOTE_ENABLED=0` set before the child starts this time), auth endpoints faked at the Playwright route layer, covers claude/gemini/qwen sign-in end to end (select → real login-launch call → refresh → "signed in", Sign in button gone, Next unblocks) and codex for state-detection only (no click, no allowance spent); asserts zero non-loopback network requests. **Readiness gate 5** — the clean-Windows-11-VM certification run — is specified step-by-step with per-step expected evidence in `docs/_journal/provider-live/w6-vm-runbook.md` (target ≤15 min from snapshot revert to both agents chatting); not run yet, needs Ron or an agent with VM access. That runbook also flags a separate installer gap found while writing it: `installer/install.ps1`'s `git clone` has no preceding git-on-PATH check, unlike its Python/Node bootstraps — not fixed here, out of this task's scope.
- Production integration matrix: **needs live restart** after branch validation and approved release handoff. No production restart or migration is performed by this plan.
- W5 (2026-09-18): full pytest suite re-run fresh at HEAD `34a1d11` (the current `origin/integrate/vendor-agnostic` tip) in an isolated worktree: **3761 passed, 10 skipped, 0 failed, 0 errors** — corrects the "32 failures at 61%" note above, which predates the W0-W4 merges that have since landed. Process exit code was 1, but the traceback is entirely inside `pytest_sessionfinish` teardown (a `PermissionError` deleting a shared, machine-wide `%TEMP%\pytest-of-levir\` symlink, most likely raced by a sibling agent's concurrent pytest run) — not a test failure. `tests/test_provider_architecture_guard.py` re-run standalone: 13/13 green, confirming Fenn's 3 escape shapes stay closed. Full detail: `docs/_journal/provider-live/cross-vendor/W5-notes.md`.
- W5 (2026-09-18): cross-vendor orchestration (plan §6) live-tested across all 6 ordered Claude/Gemini/Qwen pairs for dispatch+notify (4 pass, 2 fail — Qwen as dispatch source only), both live for roster hire, blocked by design for mixed-vendor workflow (human-only workflow authoring), confirmed not-possible for cross-vendor handoff, and found one new hivemind bug plus re-confirmed one already-known open gap. Full write-up + all evidence files: `docs/_journal/provider-live/cross-vendor/W5-notes.md`. See the `cross-dispatch`/`mixed-workflow`/`handoff`/`hivemind-mixed-workers` rows above for the per-cell summary.
