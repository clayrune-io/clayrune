# Backlog triage — the pre-2026-08-29 half of `mission_control`

**Date:** 2026-09-01 · **Scope:** the 31 live items created before 2026-08-29.
**Excluded by instruction** (already with builders or another agent):
MC-908, MC-909, MC-910, MC-911, MC-912, MC-913, MC-917, MC-918, MC-930, MC-931.

**Result:** 4 items closed with proof, 24 still valid, 3 need a decision from Ron.
This is a read-only triage — no feature code was written.

---

## Decisions Ron owes (3)

| # | Item | The question |
|---|---|---|
| 1 | **MC-873** — steward / marketing charter | Both of this charter's runners have been **off since 2026-08-10** (schedules `278d1cf5` steward, `486c367c` campaign tracker, `enabled: false`, last ran 2026-08-10), while the promotion work is now actually being done by Posy's two *enabled* daily `clayrune_website` schedules (`0566ec5b`, `2b9d5c60`). **Re-enable the two mission_control charter schedules, or retire MC-873 and let Posy's clayrune_website schedules be the charter?** |
| 2 | **MC-892** — memory index remover | You already ruled (2026-08-15) that this does not get worked inline. The route has to be picked before anyone starts: **(a)** machine eviction of the smaller 44-line variant *plus* a per-line delivery gate, or **(b)** a human-executed cut, which is the only remover with a track record here. Pick one. (Independent of that: the condense byte-deadband is still broken — see the row below — and that *is* a safe standalone fix.) |
| 3 | **MC-871** — visual workflow builder | The *viewer* half shipped (Workflows tab, `67a160a`), and Claude Code has since shipped its own `Workflow` tool with named phases plus saved scripts in `.claude/workflows/`. **Is a visual workflow AUTHORING surface still wanted, or is "use a workflow to …" plus the read-only Workflows tab enough?** |

---

## Verdicts

Legend — **A** already shipped · **B** superseded/duplicate · **C** still valid, actionable · **D** needs a decision · **E** stale.

| Item | Verdict | Evidence | Next action |
|---|---|---|---|
| **MC-874** control-plane session-JWT | **A — CLOSED** | `837d6d7` + `0781888`, both `merge-base --is-ancestor … master` = 0. All five sub-asks verified independently of MC-875: (1) `GET /v1/jwks` — `routes_auth.py:71`, ES256 keys from `jwt_es256.jwks()`; (2) `cr_session` cookie minted — `sessions.py:84`, 30-min access JWT; (3) `POST /v1/session/refresh` is the live-entitlement chokepoint — `routes_auth.py:12` plus its "Why refresh is the chokepoint" docstring; (4) **CF Access provisioning is gone** — `grep create_access_app` returns *zero* callers in the whole tree, `routes_account.py:971` records that `cf_access_app_id` is deliberately no longer written, and the surviving `delete_access_app` calls are legacy-row teardown only (`cloudflare.py:27`); (5) KV denylist — `denylist.py`, `cloudflare.py:360`. 56 tests pass (`test_session_auth*.py`, `test_edge_contract.py`; 1 skip = clayrune-cloud not checked out beside this repo). | Status → `done`. MC-875 is a *separate* ask (the APK client), not a missing part of this one. |
| **MC-905** feedback channel 2/3 — installer contact line | **A — CLOSED** | The "deliberately NOT pushed" premise is **no longer true**: `git merge-base --is-ancestor 9f0d07a origin/master` succeeds — it went out in the 2026-08-26 push. Code verified: `install.sh:62` single `trap _clayrune_exit_footer EXIT`; `install.ps1` has 12 `Exit-WithContact` sites; both print `hello@clayrune.io` (`install.sh:50`, `install.ps1:503`). | Status → `done`. Its release gate is satisfied by MC-906 below. |
| **MC-906** feedback channel 3/3 — transport | **A — CLOSED** | The blocker (no Email Routing permission) was resolved the same evening the item was filed. Cloudflare DNS API on zone `d3550…670` returns three MX records to `route{1,2,3}.mx.cloudflare.net` stamped `"meta":{"email_routing":true}`, `created_on: 2026-08-26T21:49:56Z` — 32 minutes after the item was created. **Proven end-to-end:** a probe mail sent to `hello@clayrune.io` and IMAP-verified as delivered to the destination inbox (`send_mail.py --to hello@clayrune.io --verify` → `verified: message is in the mailbox`). | Status → `done`. This also unblocks MC-904. |
| **MC-28** modal agents talking to each other | **E — CLOSED (`wontdo`)** | Premise no longer holds; the harness shipped it. `ListAgents` in this very session enumerated **9 live peer Clayrune sessions** across projects and worktrees, by address, and `SendMessage({to, message})` is the documented path to any of them. Clayrune's own coordination bus (`mc/blueprints/coordination_routes.py`, `coordination_enabled: true`) already supplies the passive half. | Status → `wontdo`, text corrected. Reopen only if a *Clayrune-side UI* for it is wanted — that is MC-887 phase 4. |
| **MC-35** progress bar on cards | **C** | Verified: `tileHTML()` (`static/js/render-core.js:323-366`) emits only `<span class="backlog-badge">N open</span>` — no bar. `backlogSummary()` (`render-core.js:48-62`) already returns both `open` and `done` from either the full array or the summary counts. | First commit: one `<div class="tile-progress">` in `tileHTML` around `render-core.js:360` plus CSS, width `done/(done+open)`, skipped when total is 0. No new endpoint, no new plumbing. |
| **MC-36** grouped list view | **C** | Verified: exactly two modes. `setView()` at `static/index.html:1539-1553` toggles `#projects-col` / `#list-view` only; the two buttons are `index.html:651-652`; command-palette entries `modal-manager.js:1010-1011`. No third mode stubbed anywhere. | First commit touches three sites: a third button, a `grouped` branch in `applyViewMode()`, and a renderer grouping `allProjects` by `friendlyStatus(p)`. |
| **MC-37** density tokens | **C** | Verified: the `body.compact` binary is intact — `index.html:1433`, `appearance.js:20-22` — and `grep -- '--pad-card\|--gap-cards\|--radius-lg' static/` returns **nothing**. | First commit: define the three tokens on `:root`, re-express the `body.compact` overrides in `static/css/app.css` as token reassignments, keep the existing toggle writing `mc_tile_density`. |
| **MC-713** in-app PWA reinstall flow | **C** | The PWA is still live and still has the described failure mode: `static/manifest.json` linked at `index.html:12`, `static/sw.js` registered at `index.html:2939`, `skipWaiting()` at `sw.js:101`. No "Installed App" settings section exists (`grep 'Installed App\|registration.update'` → one hit, inside `sw.js` itself). | Low priority. First commit: a Settings subsection reading `navigator.serviceWorker.getRegistration()` plus a "Force reload service worker" button calling `registration.update()`. |
| **MC-727** Android App Links | **C** | Re-verified on disk: `E:/clayrune-mobile/.../AndroidManifest.xml` has **no** `autoVerify`, no `BROWSABLE` filter and no `android:host`. No `assetlinks.json` is served. | Unchanged scope. Needs a public HTTPS host for `/.well-known/assetlinks.json` before the manifest change means anything. |
| **MC-728** APK branding | **C — narrowed to one file** | App name and launcher icon are done. Still stock: `res/drawable/splash.png` is 4,040 bytes dated 2026-05-08 — unchanged since the Capacitor scaffold. | One asset swap plus an APK rebuild. Smallest item in this batch. |
| **MC-735** FCM token wiring | **C — narrowed to the token half** | The Firebase *project* is wired (`google-services.json` present; both `process*GoogleServices` tasks have run). The native half is absent: `grep -rn 'FirebaseMessaging\|getToken' E:/clayrune-mobile/android/app/src/main/java/` returns **zero hits**. | First commit: a `FirebaseMessagingService` subclass that requests a token and POSTs it to MC's existing push-subscription endpoint. Project setup is not part of the remaining work. |
| **MC-868** per-turn model switching | **C — blocked by MC-895** | Dependency confirmed: the v1 auto-router shipped (`_resolve_dispatch_model`, `mc/blueprints/agent_routes.py:454`). But `docs/AGENT_TYPES_DESIGN.md:328` now files this as **phase 5 of MC-895**, "only sensible after 1–3". | Do not start before MC-895 phases 2–3. Link added: `MC-868 blocked_by MC-895`. |
| **MC-869** MCP-fleet efficiency | **C** | Verified live: still exactly the **3 pilots** — `enabled_mcp_servers` is set on `clayrune_website`, `engulfing-analyst` and `mission_control`, and on nothing else (scanned all 30 project records). Idle-eviction is on (`idle_eviction_enabled: true`, 60 min). The trim surface is `GET/PUT /api/project/<id>/mcp-enabled` (`mcp_routes.py:209-240`). | First commit is a measurement, not code: re-run the warm-session duplication count against the post-restart baseline in engram, then either (a) build #3 (shared HTTP daemon, tradingview first) or (b) set `enabled_mcp_servers` on the remaining projects. |
| **MC-870** OSS repo representation | **C — narrowed** | Half the remainder landed 2026-08-26 in `a3addd1` (on `origin/master`): `SUPPORT.md` plus `.github/ISSUE_TEMPLATE/{bug_report,feedback,config}.yml`. Still absent, confirmed by `ls`: **`CONTRIBUTING.md`, `CODE_OF_CONDUCT.md`, `SECURITY.md`, `.github/PULL_REQUEST_TEMPLATE.md`**. The README half landed long ago (`b645a33`). | Four files. `SECURITY.md` is the urgent one — the repo is public and listed on awesome-selfhosted, and there is no stated disclosure route. |
| **MC-871** visual workflow builder | **D** | Viewer half shipped: Workflows tab (`67a160a`), renderer `static/js/agent-console.js:513-576`, backend `_scan_project_workflows` + `GET /api/project/<id>/workflows` (`agent_routes.py:7128-7213`). The authoring surface and the self-feeding cycle are unbuilt. `AGENT_TYPES_DESIGN.md:4` lists MC-895 as unblocking this. | **Decision 3 above.** |
| **MC-873** steward / marketing charter | **D** | Both charter runners are disabled: `278d1cf5` (steward, last run 2026-08-10T11:48Z) and `486c367c` (campaign tracker, last run 2026-08-10T07:21Z), both `enabled: false`. The journal was last touched 2026-08-24. Meanwhile `0566ec5b` and `2b9d5c60` (Posy, clayrune_website) are enabled and firing daily. | **Decision 1 above.** MC-909 (scheduler double-dispatch, in flight) may be why these were switched off. |
| **MC-875** mobile pairing off CF Access | **C** | Re-verified on disk today: `MainActivity.java:597,691` and `FileDownloader.java:137` still set `CF-Access-Client-Id`; `grep 'cr_session\|session/refresh\|refresh_token'` across the mobile Java tree returns **zero**. The server side it has to talk to is done (MC-874, closed above), so this is now purely client work. | First commit is in `E:/clayrune-mobile`: replace the service-token header path with a `cr_session` cookie obtained from `POST /v1/session/start`, plus silent refresh. Existing pairs will have to re-pair. |
| **MC-878** tunnel quota alarm | **C** | Confirmed: `af7e259` is **documentation only** — `git show --stat` is one file, `docs/remote-access/08-tunnel-capacity-and-roadmap.md`, +10 lines. No quota code anywhere (`grep 'tunnel.*quota\|tunnel_count'` → one unrelated comment in `mc/memory.py`), and no tunnel schedule exists among the 25 live schedules. | Genuinely an afternoon: a script counting tunnels via the CF API, emailing through `tools/night-review/send_mail.py` above 800, plus a weekly `POST /api/schedules` entry. |
| **MC-879** self-hosted / BYO-tunnel path | **C** | `docs/SELF_HOSTED_REMOTE.md` does not exist. There is no `mc_remote_lan` provider — only the `mc_remote_iface` seam it would plug into (`mc/blueprints/remote_routes.py:38-99`). | Buildable today; the item's own "billing-day gate" has not arrived (standing decision: monetization deferred). First commit: the doc plus a LAN/BYO provider registered through `mc_remote_iface.get_provider()`. |
| **MC-881** optional tool-call guardrails | **C** | Confirmed still trust-based: `agent_permission_mode` (`server.py:89`) only forwards Claude Code's own `--permission-mode` (`agent_runtime.py:882`; picker at `settings-drill.js:318-324`). The one real interception layer is the steward's `PreToolUse` fence (`steward/core.py:211-330`), scoped to steward cycles. | Low priority, as filed. First commit would generalise the steward fence into an opt-in per-project `PreToolUse` hook, default off. |
| **MC-883** install.sh macOS no-Homebrew uv path | **C** | Still broken and still masked. Latest run `33017499152` (2026-08-26): `macos-no-python => failure` while the workflow reports success, because `install-smoke.yml:140` sets `continue-on-error: true`. The fragile code is `installer/install.sh:548-550` — pipes `astral.sh/uv/install.sh`, then hardcodes `PATH="$HOME/.local/bin:$PATH"` and gates on `command -v uv`; the CI assertion `ls ~/.local/bin/uv` (`install-smoke.yml:194`) is what fails. | First commit: detect uv's real install path (`XDG_BIN_HOME` / `~/.local/bin` / `CARGO_HOME`) instead of hardcoding, update the CI assertion to match, then remove `continue-on-error`. **Needs validation on the real Mac** (`ronlevy@192.168.86.225`) — CI alone cannot prove it. |
| **MC-885** completion-drive / bounded autonomy | **C** | Unbuilt as a product feature. Today it is prose in this operator's `SHARED_RULES.md`, which is gitignored and per-install — a fresh install gets none of it. No `autonomy:` key exists in `mc/characters.py`, `character_routes.py`, or any character file (checked `~/.claude/agents/dave.md`). | `docs/AGENT_TYPES_DESIGN.md:176-195` already specifies the first commit: an `autonomy: ask\|bounded\|steward` key on the character, a persona-editor row, and a matching directive emitted by `_build_agent_context`. Filed there as MC-895 phase 4 — "small code, **needs a review**, it touches the authority bright line". |
| **MC-887** cross-agent coordination | **C — correctly `in_progress`** | Phases 0–2 shipped (`141dca3`, `d30e963`, `0398915`; `agent_worktree.py`) and live — the `SIBLING ACTIVITY` block in this session's own prompt is the proof. Two named remainders: **(a) Phase 3 auto-trigger** — `sync_into()` exists at `agent_worktree.py:464` and `grep` finds **zero non-test callers**, so nothing ever propagates a sibling's landed work; **(b) Phase 4 UI panel** — the `/coord/*` API is done, no frontend consumes it. | Next commit is (a): a turn-gated step in the coordination daemon that calls `sync_into` only in the quiescent window between a sibling's turns, escalating conflicts as COORD events. The plan is already written at `docs/COORDINATION_LAYER_DESIGN.md:584-604`. |
| **MC-890** learning-loop Phase 5 (auto mode) | **C — precondition still unmet** | Re-measured today, not quoted. Soak cohort: **11/11 skills present on disk, 0 reverted, age now 61 days** — the gate (≥14d, ≤2 reverts) is still green. The precondition is still broken in **4 of 11**: `scanner-zero-detections-diagnosis`, `distinguish-health-timeout-from-outage` and `metric-denominator-audit` all carry `description: Operating procedure` with a duplicate `name:`/`description:` header emitted into the body; `why-did-fr-sentiment-agg-fail-…` has `description: opaque-cloud-run-exit-triage` — its rename target, not a description. All four are effectively un-triggerable. The promotion queue has **grown from 215 to 361** proposal directories. | Fix the reframer's duplicate-frontmatter bug and repair the four skills **before** any auto-mode work. Build order unchanged: rollback surface lands before auto-write (`docs/SKILLS_CURATION_PHASE5_AUTOMODE_ROLLBACK_SCOPE.md`). |
| **MC-892** memory index remover | **D** | Item §4 **confirmed live today**: `_over_floor` (`mc/memory.py:1958-1963`) evicts at `_index_byte_floor()` = 23,552, while the condense trigger `over_bytes` (`memory.py:1803`) needs `> _index_byte_cap()` = 24,576. The floor always bites first, so the byte trigger can never fire. The line side is correct (trigger 160 < floor 185). MEMORY.md measured at **23,465 bytes / 140 lines** — 87 bytes under the eviction floor, so the mechanical trimmer is at the threshold right now. Ron's 2026-08-15 ruling that this needs its own exploration still stands. | **Decision 2 above.** The deadband fix is separable and safe to do on its own. |
| **MC-895** agent types + per-type models | **C — phase 1 shipped** | `docs/AGENT_TYPES_DESIGN.md:3`: "Phase 1 SHIPPED 2026-08-22; Phases 2-5 design-only." Verified live: `GET /api/characters` returns typed agents carrying `provider`/`model`/`effort`/`skills`/`scope`, both global and project-scoped; full CRUD at `mc/blueprints/character_routes.py:89-531`; the persona editor is provider-aware (`5cab4de`); dispatch honours the pin (`9ee5ae2`); resume keeps the agent (`894200a`, `d68c954`). | Remaining is phases 2–5. Next commit is **phase 2 — `bindings`**, a deterministic `{project, trigger} → character` map, "small once Phase 1 lands". Phase 4 is MC-885; phase 5 is MC-868. |
| **MC-897** THE FLOOR | **C — shipped except one stage** | The item text saying "NOT BUILT" is now **false**. The Floor is live: `mc/blueprints/floor_routes.py` (`GET /api/floor` returns `rooms` / `figures` / `bench` / `quiet` / `counts`), `static/js/floor.js` (467 lines), sidebar entry `index.html:496`, mobile drawer `index.html:811`. Called it live and it returned this session's own room with 5 figures and an 11-card bench. The design's open question resolved as a sidebar entry, not an Inbox merge. **Unbuilt: stage 3, coordination edges from `coord/roster`** (`AGENT_FLOOR_DESIGN.md:245`) — the `/api/floor` payload has no `edges` key. The bench click deliberately sets the composer persona rather than dispatching (`floor.js:338`) — a design refinement, not a gap. | Text rewritten to the residue. Remaining work is one small stage. |
| **MC-898** daily field-research agent | **C** | The agent *type* exists (`market-scout` / Halloway, "nightly market CR competitor research") but **nothing fires it**: there is no field-research schedule for `mission_control` among the 25 live schedules. The nearest live thing is `99f1ae73` `[Position review]`, which is **weekly** and scoped to standing positions — not the daily what-changed-in-the-field sweep this asks for. An agent type with no schedule is not a shipped recurring job. | First commit is a `POST /api/schedules` entry (daily, `character: global:market-scout`) plus a decision on where the output lands — journal file, email, or a project doc. |
| **MC-903** DAVE | **C — phases 1–4 shipped** | Phases 1–3 as recorded (`de443bb`, `19c0b00`, `38d1ed2`, `8d7c1bf`, `mc/positions_review.py`, schedule `99f1ae73` enabled, next 2026-09-06). **Phase 4 also shipped since the item was last edited**: `dcc670f` → `d887752` → `24e3d74`, all on `origin/master`. It deliberately landed as a *weekly watch, not a screen* — the commit body carries the measurement that killed the screen (529 of 586 archive lines never delivered and already costing nothing; promotion was 2 notes across 189 tasks) and the anomaly that justified shipping it anyway (one position reaching 57% of tasks). `delivery_review.py` runs weekly inside schedule `1ba957e3`. The "there is still no Dave" line is also stale — `dave.md` is a live global character and is the persona that ran this triage. | Remaining: **phase 5, episodic search**. Note this now interacts with `decision_step7_semantic_search_deferral` — Step 7 was deferred pending search-precision telemetry, which phase 4 has now produced. |
| **MC-904** in-app "Send feedback" | **C — now unblocked** | Its stated blocker was MC-906, closed above and proven end-to-end. Nothing is built yet: `grep 'Send feedback\|sendFeedback'` across `static/` returns nothing. The GitHub issue templates from `a3addd1` do **not** satisfy this — the item explicitly says not to treat GitHub Issues as the channel. | Ready to start. First commit: an entry point (sidebar footer or project ⋮), a modal with version and OS **prefilled as visible, editable text** (the non-negotiable privacy constraint), an optional reply-to, honeypot plus rate limit, posting to `hello@clayrune.io` through the existing mailer. |
| **MC-907** browser-pane transport | **C** | Re-verified unchanged: `browser_stream()` (`mc/blueprints/browser_routes.py:717-746`) is still a polling SSE generator at `sleep(0.033)` yielding whole base64 JPEG frames; capture is still `format: jpeg, quality: 55, everyNthFrame: 1` (`browser_routes.py:359-361`). No commits to the file since 2026-08-26. | First commit is fix (1) from the item: move frame delivery to a binary transport (WebSocket, or `multipart/x-mixed-replace`), removing the +33% base64 tax outright. Do **not** lower quality — that lever was measured and rejected. |

---

## Actions taken

**Closed (4):** MC-874, MC-905, MC-906 → `done`; MC-28 → `wontdo`.

**Text corrected** where the item said something now factually wrong:
MC-28, MC-728, MC-735, MC-870, MC-878, MC-887, MC-890, MC-895, MC-897, MC-903, MC-904.

**Links added:** `MC-868 blocked_by MC-895`, `MC-871 blocked_by MC-895`,
`MC-875 relates_to MC-874`, `MC-904 relates_to MC-906`, `MC-28 relates_to MC-887`,
`MC-885 relates_to MC-895`.

**Not done, deliberately:** no feature code. MC-728 (one splash asset) and the
MC-892 condense deadband are both small enough to be tempting; both are reported,
neither was written.

Running log: `docs/_journal/backlog-triage-2026-09-01.md` (gitignored).
